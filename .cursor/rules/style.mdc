---
description: 
globs: 
alwaysApply: true
---
# UI Style Guide

- Tailwind only — no inline styles
- Preferred classes:
  - Padding: `p-4`, `p-6`
  - Radius: `rounded-2xl`
  - Font: `font-sans`
  - Shadow: `shadow-md`
- Use `shadcn/ui` components by default (buttons, inputs, cards)
- Mobile-first design required

---
description: 
globs: 
alwaysApply: true
---
# Auth Rule

- All protected API routes must check user JWT or Supabase session
- If unauthenticated, return: `401 Unauthorized`
- UI routes requiring auth must redirect to `/login` if session is invalid
- On page load, always show loading spinner while checking auth
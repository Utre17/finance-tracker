---
description: 
globs: 
alwaysApply: true
---
# UI Component Rule

- Always use functional components
- Each UI feature must be broken into atomic components
- File placement:
  - /components/ → Reusable UI elements
  - /hooks/ → Logic-only custom hooks
  - /utils/ → Pure helper functions
- Do not mix logic and UI in the same file

